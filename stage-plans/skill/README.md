# Skill 计划

本目录只作为 skill 相关工作的协作入口。

skill 的阶段计划、迁移记录、运行时后续清单只在进行中时放在这里，避免和 role、memory、commander 或通用实现计划混在一起。已经完成的阶段计划不继续保留，防止后续并行进程误读状态。

当前边界：

- skill spec 和第一版执行器代码位于 `src/skills`。
- 第一阶段旧 Python skill 迁移已完成，原迁移 checklist 已删除。
- 第二阶段 Rust runtime 收口已完成，阶段 plan 和 status 已删除。
- 当前没有活动中的 skill plan。
- 做 skill 工作时，不修改 role、memory、commander 或通用 stage plan。
